@model IEnumerable<ProyectoMVCReal.Models.DEPT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo Departamento", "InsertarDepartamento","DeptLinq")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DEPT_NO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DNOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LOC)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DEPT_NO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DNOMBRE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LOC)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>

@section sweetAlert{<script></script>}